VIEW hierarchy_parent_child AS ( 
	SELECT 
		DISTINCT CITY_CODE as CHILD, COUNTRY_CODE as PARENT
	FROM HIERARCHY
	UNION ALL
	SELECT 
		DISTINCT COUNTRY_CODE as CHILD, CONTINENT_CODE as PARENT
	FROM HIERARCHY
	UNION ALL
	SELECT 
		DISTINCT CONTINENT_CODE as CHILD, 'ROOT' as PARENT
	FROM HIERARCHY
)